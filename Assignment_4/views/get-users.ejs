<%- include('includes/head.ejs') %>

<body>
    <%- include('includes/nav.ejs') %>

    <main>
       <% if (users.length > 0) { %>
            <ul>
                <% for (let user of users) { %>
                    <li> <%= user.title %> </li>
                <% } %>
            </ul>
        <% } else {%>
            <h1> No Users Found! </h1>
        <% } %>
    </main>
</body>
</html>